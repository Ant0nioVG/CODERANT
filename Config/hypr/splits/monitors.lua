-- ============================================================
-- Monitor configuration
-- Monitors are auto-detected by Hyprland; no static outputs.
-- Override per machine with hyprctl monitors if needed.
-- ============================================================

hl.monitor({
    output   = "",
    mode     = "preferred",
    position = "auto",
    scale    = "auto",
})
