# ============================================================
# Kitty terminal emulator - entry point
# Splits configuration into separate theme / font / cursor /
# keymap / window / tab-bar files.
# ============================================================

include themes/matugen-theme.conf
include splits/font.conf
include splits/cursor.conf
include splits/keymaps.conf
include splits/general.conf
include splits/window.conf
include splits/tabbar.conf
